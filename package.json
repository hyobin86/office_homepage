{
  "name": "fingate-homepage",
  "private": true,
  "scripts": {
    "build": "npm run generate:seo && nuxt build",
    "dev": "nuxt dev",
    "generate": "npm run generate:seo && nuxt generate",
    "generate:seo": "npm run generate:sitemap && npm run generate:robots",
    "generate:sitemap": "node scripts/generate-sitemap.js",
    "generate:robots": "node scripts/generate-robots.js",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "clean": "rmdir /s /q .nuxt .output .nitro .cache dist 2>nul || echo Cleaned",
    "type-check": "nuxt typecheck"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "@nuxt/image": "^1.11.0",
    "nuxt": "^3.8.0",
    "sass": "^1.93.1",
    "typescript": "^5.3.0",
    "vue-tsc": "^3.0.8"
  },
  "dependencies": {
    "@emailjs/browser": "^4.4.1",
    "@pinia/nuxt": "^0.5.1",
    "@vueuse/nuxt": "^10.5.0",
    "gsap": "^3.13.0",
    "lenis": "^1.3.11",
    "pinia": "^2.1.7",
    "vue": "^3.4.0"
  }
}
